# ── Server API key — used by BOTH CLI scripts AND Next.js API routes ──────────
#
# Get a SERVER key (sk_ prefix) from console.crossmint.com
# Required scopes: wallets.create, wallets.read, wallets.fund, wallets:transactions.create
#
# WHY the Next.js UI also needs a server key:
#   This POC uses the "api-key" custodial signer type. With this signer the
#   server API key IS the signing authority — Crossmint signs UserOperations on
#   your behalf using your key. There are no client-accessible endpoints for
#   this signer type; all wallet operations must go through server-side code
#   (Next.js API routes in the UI, CLI scripts on the command line).
#   Token balance reads are the only thing that bypasses Crossmint entirely
#   (they go direct to the chain via viem).
#
CROSSMINT_SERVER_API_KEY=sk_production_...

# Wallet locator - auto-created if not set
# Format: userId:<your-user-id>:evm-smart-wallet
# Format: userId:<userId>:<chainType>[:<walletType>][:alias:<alias>]
CROSSMINT_WALLET_LOCATOR=userId:demo-user:evm:smart

# ── LI.FI (shared) ────────────────────────────────────────────────────────────
# LI.FI SDK integrator identifier (used for analytics)
LIFI_INTEGRATOR=crossmint-lifi-poc

# Optional: LI.FI API key for higher rate limits
# Get one at: https://li.fi/sdk
# LIFI_API_KEY=

# Note: Fund the wallet address with a small amount of USDC on Base before
# running the bridge demo (both CLI and UI share the same smart wallet address)
